# Generate Helix editor configuration file from Avon
# Demonstrates generating TOML config files

\theme ? "dark_plus"
\insert_shape ? "bar"
\normal_shape ? "block"
\select_shape ? "underline"
\c_space_key  ? "normal_mode"

@.config/helix/config.toml {"
    theme = "{theme}"

    [editor.cursor-shape]
    insert = "{insert_shape}"
    normal = "{normal_shape}"
    select = "{select_shape}"

    [keys.insert]
    C-space = "{c_space_key}"
"}
